# ai_1.0

An experimental repo for testing [flow](https://github.com/nazanindev/ai-flow), an AI agent harness using Claude Code's agentic flow to autonomously plan, implement, and ship features across phases.

## About

This repo is a sandbox for exploring what an AI agent can build end-to-end with minimal human intervention. Each phase targets a different problem domain to stress-test the harness across varied codebases and requirements.

## Phases

### Phase 1 — Python CLI Games

Simple terminal games generated by the agent to validate the basic build-and-ship loop.

| Game | Command | Description |
|------|---------|-------------|
| Texas Hold'em Poker | `python -m poker` | ANSI card art, AI opponents, full betting rounds |
| 5-Card Draw Poker | `python -m five_card_draw` | Interactive draw phase with AI opponents |
| Blackjack | `python -m blackjack` | Hit, stand, double down |
| Roulette | `python -m roulette` | European single-zero, 6 bet types |
| Pai Gow Tiles | `python -m pai_gow` | Ancient Chinese domino casino game |
| Bunny Hop | `python -m bunny_hop` | Grid game: collect carrots, dodge foxes |

Each game has its own `README.md` with full instructions.
```bash
python -m pai_gow
```

See [`pai_gow/README.md`](pai_gow/README.md) for full instructions.
### 🂠 5-Card Draw Poker CLI

A terminal 5-Card Draw poker game with ANSI card art, AI opponents, and an interactive draw phase.

```bash
python -m five_card_draw
```

See [`five_card_draw/README.md`](five_card_draw/README.md) for full instructions.
### Blackjack CLI

Classic blackjack with hit, stand, and double down.

```bash
python -m blackjack
```

See [`blackjack/README.md`](blackjack/README.md) for full instructions.

### 🐰 Bunny Hop

A cute terminal game: navigate your bunny around a grid to collect carrots while dodging foxes. Arrow keys to move, foxes get faster every level.

```bash
python -m bunny_hop
```

See [`bunny_hop/README.md`](bunny_hop/README.md) for full instructions.
### Roulette CLI

European single-zero roulette. Bet on a number, color, parity, half, dozen, or column — then spin the wheel.

```bash
python -m roulette
```

Bet types and payouts:
- **Straight** (0-36) — pays 35:1
- **Color** (red/black) — pays 1:1
- **Parity** (even/odd) — pays 1:1
- **Half** (low 1-18 / high 19-36) — pays 1:1
- **Dozen** (1st/2nd/3rd) — pays 2:1
- **Column** (1st/2nd/3rd) — pays 2:1

See [`roulette/README.md`](roulette/README.md) for full instructions.

### ♠ ♥ Contract Bridge CLI

The classic four-player trick-taking card game. Play South against AI opponents, bid your contract, then fight to make it through 13 tricks.

```bash
python -m bridge
```

Features: ANSI-colored compass table layout, full duplicate scoring (game/slam bonuses, doubled undertricks), and AI bidding based on standard point-count conventions.

See [`bridge/README.md`](bridge/README.md) for full instructions.
